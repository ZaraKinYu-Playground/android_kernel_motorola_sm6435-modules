ifeq ($(ANDROID_BUILD_TOP),)
ANDROID_BUILD_TOP := $(srctree)/..
endif

# add -Wall to try to catch everything we can.
EXTRA_CFLAGS += -Wall
EXTRA_CFLAGS += -I$(ANDROID_BUILD_TOP)/kernel-5.10/include
EXTRA_CFLAGS += -I$(KERNEL_SRC)/../sm6435-modules/motorola/include \
		-I$(ANDROID_BUILD_TOP)/kernel_device_modules-6.1/include \

# KBUILD_EXTRA_SYMBOLS += $(OUT_DIR)/Module.symvers
# KBUILD_EXTRA_SYMBOLS += $(CURDIR)/../../kernel_platform/msm-kernel/Module.symvers

obj-m += mmi_lpm_dbg.o
